{
  "scripts": {
    "start:server": "nodemon server/index.js --watch server --watch models --ignore client",
    "start:client": "npm start --prefix client/empts_client",
    "dev": "concurrently \"npm run start:server\" \"npm run start:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
